policy_name: ats_policy_v1
version_tag: 2025-12-28_away_only_certified

# Locked OOS windows (documentation + provenance)
oos_train_window:
  start: 2023-10-24
  end: 2024-03-10
oos_eval_window:
  start: 2024-03-11
  end: 2024-04-14

# Current observed performance under AWAY-ONLY (eval window)
oos_observed:
  roi_eval_window: 0.0309
  win_rate_eval_window: 0.54
  bets_eval_window: 50
  bet_rate_eval_window: 0.186
  note: "Eval window 2024-03-11..2024-04-14 with side_policy=away_only, ev_threshold=0.03, max_dispersion=2.0."

# Policy parameters (LOCKED v1)
side_policy: away_only
ev_threshold: 0.03
max_dispersion: 2.0

# Execution pricing semantics (documentation)
# - Selection uses calibrated P(cover) + EV gating
# - Settlement / certification uses market open spread prices and close grading
pricing: market_open_spread_price

# Explicitly disabled in v1 (by design)
min_abs_residual: 0.0

# Guards
guards:
  max_bet_rate: 0.30
  max_profit_abs: 10.0
  fail_on_overlap: false

# Certification snapshot (E2 pass)
e2_certified_summary:
  bets: 217
  roi: 0.1708
  average_clv: 0.846635
  clv_positive_rate: 0.641
  max_drawdown_units: -6.357
  eligible_pct: 0.958
  note: "From qa/certify E2 pass (ATS-based E2 runner + real snapshot open/close spread CLV)."

